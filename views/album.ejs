<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <!-- Police -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Splash&display=swap" rel="stylesheet"> 

    <!-- Style -->
    <link rel="stylesheet" href="/reset.css">
    <link rel="stylesheet" href="/album.css">

    <!-- Icone -->


    <!-- Javscript -->
    <script defer src="/album.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/animejs/2.0.2/anime.min.js"></script>

    <title>Album <%= titre  %>  </title>
</head>
<body>

    <header>
        <nav>
            <a href="/home">Galerie</a>
            <a href="/contact">Contact</a>
            <div>
                <p>arty</p>
            </div>
        </nav>
    </header>
    
    <main>

        <% if (titre == "noir-et-blanc") { %>

            <h1 class="ml12"> Album noir et blanc  </h1>
            
        <% } else { %>

            <h1 class="ml12"> Album <%= titre %>  </h1>

        <% } %> 
            

        <section class="galerie">


            <% for( let index = 0; index < images.length; index++ ) { %>

                <% if(images[index]['categorie'] == titre ) {%> 
                    <div>
                        <img src="<%= images[index]['urlFileCompressed'] %>" alt="<%= images[index]['alt'] %>">
                        <div class="opaque">
                            <a target="_blank" href="<%= images[index]['urlDownload'] %>">Télécharger</a>
                        </div>
                    </div>
                <% } %>

            <% } %>

            
        </section>


    </main>

    <footer>

    </footer>

</body>
</html>